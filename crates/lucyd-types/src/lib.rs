//! Shared types for the Lucyd documentation framework.
//!
//! This crate is the single source of truth for all data structures
//! exchanged between the proc-macro crate (`lucyd-macro`) and the
//! runtime crate (`lucyd-core`).

pub mod endpoint;
pub mod schema;
